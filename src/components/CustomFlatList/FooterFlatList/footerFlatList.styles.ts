import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  footerContainer: {
    height: 200,
    justifyContent: 'center',
  },
  loaderContainer: {
    height: 100,
    justifyContent: 'center',
  },
});
